#pragma once
enum dim { windowx = 760, windowy = 980, blocksize = 75 };// the enum with the values used for the window x and y size and blocksize is for character size
void game_init(void);
void game_update(void);
void game_exit(void);